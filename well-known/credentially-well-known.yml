generated: '2026-07-18'
method: probed
note: >-
  Probed the /.well-known/ discovery surface on the marketing host (credentially.io)
  and the API host (app.credentially.io). No real discovery documents are published:
  credentially.io returns 404, and app.credentially.io returns the SPA index.html shell
  (soft-404) for every /.well-known/ path rather than a JSON/text document. No pointer wired.
hosts:
- host: https://credentially.io
  documents:
  - {path: /.well-known/security.txt, status: 404}
- host: https://app.credentially.io
  documents:
  - {path: /.well-known/security.txt, status: 200, result: soft-404 (SPA index.html, not RFC 9116 text)}
  - {path: /.well-known/openid-configuration, status: 200, result: soft-404 (SPA index.html, not OIDC JSON)}
  - {path: /.well-known/oauth-authorization-server, status: 200, result: soft-404 (SPA index.html, not RFC 8414 JSON)}
